[project]
name = "openparkcad"
version = "0.3.0"
description = "Minimal Python parking layout generator for irregular lots."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "ezdxf>=1.3",
  "shapely>=2.0",
]

[project.optional-dependencies]
optimizer = [
  "ortools>=9.10",
]
dev = [
  "build>=1.2",
  "coverage[toml]>=7.6",
  "jsonschema>=4.23",
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "wheel>=0.44",
]

[project.scripts]
openparkcad = "openparkcad.cli:main"

[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["openparkcad*"]
exclude = ["examples*", "output*", "tests*"]

[tool.setuptools.data-files]
"share/openparkcad/schema" = ["schema/openparkcad-input.schema.json"]
"share/openparkcad/fixtures/v0_3" = ["tests/fixtures/v0_3/*.json", "tests/fixtures/v0_3/README.md"]
"share/doc/openparkcad" = ["CHANGELOG.md", "docs/v0.3_vehicle_and_constraints.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
branch = true
data_file = ".pytest_cache/.coverage"
source = ["openparkcad"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = true

[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = ["output"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
